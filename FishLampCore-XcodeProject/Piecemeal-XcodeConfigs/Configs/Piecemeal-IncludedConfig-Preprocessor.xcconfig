
// this is here because of limitations in how the config stuff works with overriding values

PIECEMEAL_PREPROCESSOR_DEFINITIONS = $(PIECEMEAL_SETTINGS) $(PIECEMEAL_DEBUG) $(PIECEMEAL_EXTRA_PREPROCESSOR)

GCC_PREPROCESSOR_DEFINITIONS = $(INHERITED) $(PIECEMEAL_PREPROCESSOR_DEFINITIONS)
